const { createServer } = require("tls");

findGreater =(x,y)=>{
    return x>y? "X is greater than y": x==y? "X is equal Y":"X is less than Y";
}

console.log(findGreater(10,5));

array.forEach(element => {
    createServer();
    
});